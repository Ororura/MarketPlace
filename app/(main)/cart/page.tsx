import { FC } from "react";

import { CartWidget } from "@/widgets/cartWidget/ui";

const CartPage: FC = () => {
  return (
    <main>
      <CartWidget />
    </main>
  );
};

export default CartPage;
