import { FC } from "react";

import { LoginWidget } from "@/widgets/loginWidget/ui";

const LoginPage: FC = () => {
  return (
    <main>
      <LoginWidget />
    </main>
  );
};

export default LoginPage;
